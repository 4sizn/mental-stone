name: mental_stone_ui
description: "Mental Stone — Ethereal Clarity design system (theme + glass widgets)."
version: 0.1.0
publish_to: "none"
resolution: workspace

environment:
  sdk: ^3.11.5
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
